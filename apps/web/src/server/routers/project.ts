import { z } from "zod";

import { createTRPCRouter, protectedProcedure } from "../index";

// Basic router structure, implementations will be added later
export const projectRouter = createTRPCRouter({
    // Project CRUD operations will be implemented here
});